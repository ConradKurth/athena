# direnv config for the Athena workspace.
# Puts the Athena CLI wrappers on PATH and loads brand credentials.
# Run `direnv allow` once after cloning. No direnv? `source scripts/env.sh`.
export ATHENA_DIR="$PWD"
PATH_add bin
if [ -f .env ]; then
  dotenv .env
fi
